import React from 'react';
import ReactDOM from 'react-dom';

const StatsPage = () => {
  return (
    <main>
      <h1>SpotifyTrack</h1>
      <p>100% under construction! Check back later.</p>
    </main>
  );
};

const rootElement = document.getElementById('root')!;
ReactDOM.render(<StatsPage />, rootElement);
